spring:
  application:
    name: Vendora

  profiles:
    active: dev

security:
  jwt:
    secret: ${JWT_SECRET}
    access-token-ttl-seconds: 900
    refresh-token-ttl-seconds: 604800
  rate-limit:
    login:
      max-attempts: 10
      window-seconds: 60

server:
  port: 8080
